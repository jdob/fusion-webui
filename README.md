# Developer Installation

* Node.js is required.
* Change to the ``webui`` directory.
* For a fresh checkout, the dependencies need to be installed:

``npm install``

* To start the server:

``npm start``
