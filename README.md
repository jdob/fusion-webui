# Developer Installation

* Node.js is required.
* For a fresh checkout, the dependencies need to be installed:

``npm install``

* To start the server:

``npm start``

# To run inside a container and pass environment variables

* Create .env.local file with the variable name and pass some value
* Make the file run executable by running the command:

 ``chmod +x run``
* Run the image with the variable
